#!/home/euphoria-4.0b2/bin/eui

-- Testando comentario.

/***** testando um comentario de bloco
 pra ver se o compilador pode
 ignorar tudo isso sem problemas*/

/*
  *aaaaa
  *bbbbb
  */

-- testando declarar variaveis.
--Tipo errado switch:
sequence b = { 0 , 1}
atom nota = 'C'

switch b do

   case 'A' then
      puts(1, "Excelente!\n" )
   case 'B', 'C' then
      puts(1, "Muito bom!\n" )
   case 'D' then
      puts(1, "Aprovado!\n" )
   case 'F' then
      puts(1, "Oh, nao! Fui reprovado!\n" )
   else
      puts(1, "Nota invalida!\n" )
end switch

integer counterwwwwww
atom miles = 0
integer a = 5
sequence name -- = {1, "aaa", 2}

atom letra
name = miles

for letra = 1 to {1,2,3} do
	atom abcd
end for

function tmp(integer a)
	atom x = 0
	sequence d
	x = d
	if a>0 then
		sequence b
		atom x, e
		b = {1,2}
		return x, e
	end if
	return x, x
end function

/*sequence s1 = {1,2,3}
sequence s2 = {1,2}
a = s1
if (s1 > s2) then
	atom alfa
	sequence b
	alfa = b
end if*/

return 1,2

--miles, name = tmp(a), s1 -- ta ok
--miles, name = tmp(a), miles -- ta ook :D ps.: nao atribuir abaixo ahahah da erro
--name = tmp(a)
--miles = tmp(a)
--miles, miles = tmp(a)