#!/home/euphoria-4.0b2/bin/eui

-- Testando comentario.

/***** testando um comentario de bloco
 pra ver se o compilador pode
 ignorar tudo isso sem problemas*/

/*
  *aaaaa
  *bbbbb
  */

-- testando declarar variaveis.
integer counterwwwwww
atom miles = 0
integer a = 5
sequence name -- = {1, "aaa", 2}

integer lalala
lalala = name + miles

/*function tmp(integer a)
	atom x = 0
	if a>0 then
		atom x = 0
		sequence b,d
		b = {1,2}
		d = {2}
		return x
	end if
	return x
end function*/

sequence s1 = {1,2,3}
sequence s2 = {1,2}
if (s1 > s2) then
	atom alfa
end if

return 1,2

--miles, name = tmp(miles), miles
--name = tmp(miles)
miles = tmp(a)
